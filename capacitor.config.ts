import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.academico.gestaocomercial',
  appName: 'Sistema Gestão Comercial',
  webDir: 'www'
};

export default config;
